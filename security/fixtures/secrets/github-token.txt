# FAKE/SYNTHETIC FIXTURE — never use this value.
#
# The 36-character body is the exact synthetic value Gitleaks 8.30.1 matches
# against the `github-pat` rule (mixed case + digits so entropy is high
# enough to clear the scanner's gate). The body is still synthetic — no
# real GitHub PAT matches this exact pattern — and the production scan
# passes `--redact=100`, so the value never reaches captured output.
ghp_aB3dE5fG7hJ9kL2mN4pQ6rS8tU0vW1xY3zA5