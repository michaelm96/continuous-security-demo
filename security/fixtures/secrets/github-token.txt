# FAKE/SYNTHETIC FIXTURE — never use this value.
#
# The 36-character body is a string of zero digits. Gitleaks matches the
# shape (ghp_ + 36 chars), so the github-pat rule fires; the value cannot
# be confused with a real token, and --redact=100 means it never appears in
# captured output regardless.
ghp_0000000000000000000000000000000000